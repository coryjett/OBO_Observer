# Set your Solo Enterprise license key as an environment variable before running demo_env.sh:
#   export AGENTGATEWAY_LICENSE_KEY="<your-solo-enterprise-license-key>"
#
# Optional: override cluster context (default: k3d-obodisplayer)
#   export KUBE_CONTEXT=k3d-obodisplayer
#
# Alternatively, copy this file to .env and set values there; demo_env.sh loads .env if present.
